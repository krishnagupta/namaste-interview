mechanism where one web app can share resources with another

origin1 origin2
preflight call
additional http header (options) accept control allow origin header is set by server
actual call is made

- any domain outside of that domain can access it
